接线方式：SDA接P1.7，SCL接P1.6

操作方式：AT24C02_Init();         使用前需先使用此函数初始化


          DATA=DS1302_Read(a);    使用此函数读取数据，其中a为8位地址（小于256）

 
          DS1302_Write(a,b);      使用此函数存储数据，其中a为地址，b为8位数据
           

使用中断：无


使用晶振：12M
