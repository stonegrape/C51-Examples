接线方式：VCC接正极，GND接地，DQ接P2.0


操作方式： DATA=DS18B20_GetTmp(); 使用时用此函数获取当前温度(精确到0.1度)。温度
                                  值储存在DATA中。注意，两次获取温度的时间间隔不得少于1s。
                                  例如：返回254，即为25.4度



使用中断：无


使用晶振：11M（12M也可以使用）